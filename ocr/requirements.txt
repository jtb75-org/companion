# PaddleOCR HTTP service deps. Pinned for reproducible kaniko builds.
# paddlepaddle is the CPU wheel (no GPU on the K3s Minisforum nodes).
# paddle imports setuptools/pkg_resources at runtime; slim images don't ship it.
setuptools==69.5.1
wheel==0.43.0
paddleocr==2.7.3
paddlepaddle==2.6.2  # 2.6.1 has no py3.12 wheel; 2.6.2 is lowest available
fastapi==0.111.0
uvicorn[standard]==0.30.1
pymupdf==1.24.5
pillow==10.3.0
pillow-heif==0.16.0
python-multipart==0.0.9
numpy==1.26.4
